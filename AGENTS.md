---
trigger: always_on
---

# チャット
日本語で行うこと。問い合わせに対しては日本語で返答すること

# プラン作成
日本語で行うこと。
プラン作成後に別のAIモデルを選択してもコンテキストが引き継がれるように、基本設計と詳細設計の両方を網羅してプランニングすること。

# 作業で必ず実施すること
- 作業完了後はtestとlintを実行し、エラーが無い状態にする。

# テーブルを変更する場合
テーブル定義書を/doc/tables/definitionディレクトリにテーブル単位に保存する。
DDLファイルを/doc/tables/ddlディレクトリにテーブル単位に保存する。
既存のカラムを変更する場合は新しいマイグレーションファイルを作成すること。

# テストについて
実装した部分のカバレッジをとれるようにテストコードを追加すること。